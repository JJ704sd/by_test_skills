interface:
  display_name: "Handoff"
  short_description: "Create a portable handoff for another agent or session"
  default_prompt: "Use $handoff to prepare a portable handoff for a fresh agent continuing this work."
policy:
  allow_implicit_invocation: false
