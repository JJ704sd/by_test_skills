interface:
  display_name: "To Tickets"
  short_description: "Split approved work into implementation tickets"
  default_prompt: "Use $to-tickets to split this approved plan into dependency-linked implementation slices."
policy:
  allow_implicit_invocation: false
