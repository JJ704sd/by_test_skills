interface:
  display_name: "Plan Engineering Work"
  short_description: "Map, specify, or slice tracker-backed work"
  default_prompt: "Use $plan-engineering-work in exactly one map, spec, or slice mode for this planning request."
policy:
  allow_implicit_invocation: false
