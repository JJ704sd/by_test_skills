interface:
  display_name: "Triage"
  short_description: "Classify and advance inbound issues and PRs"
  default_prompt: "Use $triage to review and advance these inbound issues through the configured triage states."
policy:
  allow_implicit_invocation: false
